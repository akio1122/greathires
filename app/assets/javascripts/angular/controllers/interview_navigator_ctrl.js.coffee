app.controller "InterviewNavigatorCtrl", ["$scope", "$modal", "$stateParams", \
                            ($scope, $modal, $stateParams) ->
  # --- Internal Use Variables ---

  # --- Scoped Variables ----  
  $scope.job_candidate = $scope.preload_resources.candidate

  # --- Methods --- 

  # --- Scoped Methods ---

]